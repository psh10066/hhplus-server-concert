spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        highlight_sql: true
logging:
  level:
    kr.hhplus.be.server.support.aop.lock: debug
    org.springframework.orm.jpa.JpaTransactionManager: debug